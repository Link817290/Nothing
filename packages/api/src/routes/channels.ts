// Channel routes
// GET    /api/channels              列出所有渠道
// POST   /api/channels              添加渠道（邮箱配置）
// PATCH  /api/channels/:id          更新渠道配置
// DELETE /api/channels/:id          删除渠道
// POST   /api/channels/:id/test     测试渠道连接（SMTP + IMAP）
// PATCH  /api/channels/:id/primary  设为默认渠道
